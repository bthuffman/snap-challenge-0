function addNumbers (parameter1,parameter2) {
	let added = parameter1 + parameter2;
	document.write(added + "<br>");
}

addNumbers(1,2);

function concatenate(starship,spacestation) {
	document.write(starship + " to space station " + spacestation + " one come in please.");
}
concatenate("Enterprise","Regula");

function subtractNumbers (parameter1,parameter2) {
	let subtracted = parameter1 - parameter2;
	alert(subtracted);
}

subtractNumbers(1,2);

first_value = 1;
second_value = 2;
thrid_value  = 3;
result = first_value + second_value - thrid_value;
print(result);